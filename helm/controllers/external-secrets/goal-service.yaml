apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: goal-service-secret
  namespace: nutri-track
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: goal-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: nutritracker/dev/goal-service
