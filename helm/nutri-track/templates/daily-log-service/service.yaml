apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dailyLog.service.name }}
  labels:
    app: {{ .Values.dailyLog.name }}
spec:
  type: {{ .Values.dailyLog.service.type | default "ClusterIP" }}
  selector:
    app: {{ .Values.dailyLog.name }}
  ports:
    - name: {{ .Values.dailyLog.service.portName | default "http" }}
      protocol: {{ .Values.dailyLog.service.protocol | default "TCP" }}
      port: {{ .Values.dailyLog.service.port }}
      targetPort: {{ .Values.dailyLog.service.targetPort }}
