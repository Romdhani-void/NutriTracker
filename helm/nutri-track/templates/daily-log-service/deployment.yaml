apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.dailyLog.name }}
  labels:
    app: {{ .Values.dailyLog.name }}
spec:
  replicas: {{ .Values.dailyLog.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.dailyLog.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.dailyLog.name }}
    spec:
      containers:
        - name: {{ .Values.dailyLog.name }}
          image: {{- if .Values.global.imageRegistry }} {{ printf "%s/%s:%s" .Values.global.imageRegistry .Values.dailyLog.image.repository .Values.dailyLog.image.tag | quote }} {{- else }} {{ printf "%s:%s" .Values.dailyLog.image.repository .Values.dailyLog.image.tag | quote }} {{- end }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.dailyLog.port }}
          envFrom:
            - secretRef:
                name: {{ .Values.dailyLog.name }}-secret
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
