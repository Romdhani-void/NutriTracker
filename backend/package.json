{
  "name": "nutri-track-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend microservices for NutriTrack",
  "scripts": {
    "install:all": "cd daily-log-service && npm install && cd .. & cd user-service && npm install && cd .. & cd user-service && npm install && cd .. & cd ",    "dev:daily": "cd daily-log-service && npm run dev",
    "dev:user": "cd user-service && npm run dev",
    "dev:goal": "cd goal-service && npm run dev",
    "go": "concurrently \"npm run dev:daily\" \"npm run dev:user\" \"npm run dev:goal\""  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
